datasource db {
  provider = "postgresql"
  url      = "postgresql://testdb_owner:TVcus2aSU8eA@ep-purple-cherry-a5ovkfyg.us-east-2.aws.neon.tech/testdb?sslmode=require"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  userId   Int     @id @default(autoincrement())
  username String  @unique
  password String
  name     String? // Optional
  Blog     Blog[] // Relation to Blog model
}

model Blog {
  blogId      Int    @id @default(autoincrement())
  title       String
  description String
  userId      Int
  user        User   @relation(fields: [userId], references: [userId])

  @@index([userId])
}
